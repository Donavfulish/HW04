{
  "name": "hw04-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for HW04 (student 23127044)",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list --pass-with-no-tests",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^26.3.0",
    "typescript": "^5.9.3"
  }
}
